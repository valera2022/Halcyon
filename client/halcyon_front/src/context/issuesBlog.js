
//session content not louded{solved: need to use proxy and use porxy names, instead of localhost/clasess use /classes}
// Body of PATCH request not showing up on params {solved: typo on headers, had header instead of headers}